import ProductCard from "./ProductCard";

const ProductList = () => {
  return (
    <section className="productList">
      <ProductCard />
      <ProductCard />
    </section>
  );
};

export default ProductList;